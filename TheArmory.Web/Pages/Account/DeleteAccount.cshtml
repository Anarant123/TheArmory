@page
@model DeleteAccount

@{
    ViewData["Title"] = "Удалить аккаунт";
}
@await Html.PartialAsync("ErrorModelWindow", Model.Result)

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow-sm">
                <div class="card-body text-center">
                    <h2 class="card-title text-danger">Удалить аккаунт</h2>
                    <p class="card-text">
                        При удалении вашего аккаунта все ваши объявления также будут удалены с площадки. Вы уверены, что хотите удалить свой аккаунт?
                    </p>
                    <form method="post" asp-page-handler="Delete">
                        <button type="submit" class="btn btn-danger btn-block w-100"><i class="bi bi-trash"></i> Удалить аккаунт</button>
                    </form>
                    <div class="mt-3">
                        <a asp-page="/Account/PersonalInfo" class="btn btn-outline-secondary w-100"><i class="bi bi-arrow-left"></i> Отмена</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>